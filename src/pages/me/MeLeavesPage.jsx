import LeavesPage from '../leave/LeavesPage';

/** Employee self-service leave portal — own balances, apply, requests only. */
export default function MeLeavesPage() {
  return <LeavesPage selfService />;
}
